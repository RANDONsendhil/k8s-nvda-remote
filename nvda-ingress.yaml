# @format

apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP

metadata:
  name: nvda-server-ingress

spec:
  entryPoints:
    - nvda

  routes:
    - match: HostSNI(`*`)

      services:
        - name: nvda-server-relay
          port: 6837
